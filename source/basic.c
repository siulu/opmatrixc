#include <stdio.h>
#include <stdlib.h>
#include <math.h>



double get_det(double *initArray) {
    double result;
    int raw = sizeof(initArray) / sizeof(initArray[0]);
    int column = sizeof(initArray)/row;
    if (raw != column) result = 0.0;
    else {
        
    }
    return result;
}